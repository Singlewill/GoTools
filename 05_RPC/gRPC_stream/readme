一、gRPC流分三种：
1，服务器流（客户端->服务器数据为单次模式，服务器->客户端数据为流模式）
2，客户端流（客户端->服务器数据为流模式，服务器->客户端数据为单次模式）
3, 双向流
这里只给出了客户端流的实例
二、protobuf文件编译选项跟之前有差别，需要加上"plugins=grpc"
#protoc --go_out=plugins=grpc:. ./define.proto
