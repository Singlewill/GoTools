1, func parse() 展示了向函数传入*People参数，在函数内通过反射修改外部变量的例子
2, func parse2() 展示了断言，判断传入参数的类型，
2, func parse3() 展示了通过反射，创建另一个同类型的变量, 也用到了断言
4, func slice_test() 展示了通过反射创建切片，追加切片，不可避免也用到了断言
5, func slice2_test() 展示了通过传入切片，追加切片

eg : 	1，涉及要新变量，都需要断言判定, 这也是golang1.18泛型解决的问题
	2，其他还有通过反射创建chan, map等，逻辑类似slice
	3, 由于效率问题，工程中还是不要用反射了，推荐用AST生成